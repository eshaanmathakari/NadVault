{
  "name": "nadvault",
  "version": "1.0.0",
  "description": "A decentralized application built on Monad testnet featuring mystery boxes, wallet analysis, and time-locked vaults",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network monad-testnet",
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.4",
    "ethers": "^6.10.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "chai": "^4.3.7",
    "ethereum-waffle": "^4.0.10"
  }
} 